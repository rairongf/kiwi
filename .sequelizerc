const path = require('path');

module.exports = {
    config: path.resolve(__dirname, "database", "src", "config", "database.js"),
    "migrations-path": path.resolve(__dirname, "database", "src", "data", "migrations"),
}